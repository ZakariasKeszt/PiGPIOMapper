# PiGPIOMapper

A lightweight C library for Raspberry Pi that uses `mmap()` to directly manipulate GPIO registers. This bypasses the slower sysfs interface for high-performance hardware control.

___
*In development.*
